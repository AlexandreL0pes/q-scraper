const browser = require('../scraper/browser');
const { login } = require('../scraper/scrap');

modules.exports = {
  async index(req, res) {
    const { page } = await browser();

    try {
      
    } catch (error) {
      
    }
  },
};
